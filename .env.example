# Copy to .env for local development. Never commit the real file.

# Format-checked at startup. Generate with: npx emdash encryption-key generate
EMDASH_ENCRYPTION_KEY=

# Only needed if you enable bindings that have no local emulator (AI Search).
# Prefer `npx wrangler login` for interactive use.
# CLOUDFLARE_API_TOKEN=
